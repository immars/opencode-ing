# 飞书配置
# 从飞书开放平台获取应用凭证: https://open.feishu.cn/

# 飞书应用凭证 (必填)
app_id: ''
app_secret: ''

# 长连接 WebSocket 配置 (推荐)
connection:
  # 启用长连接
  enabled: true
  # 自动重连间隔 (毫秒)
  reconnect_interval: 5000

# 消息配置
message:
  # 监听的群组 ID 列表 (留空则监听所有)
  group_ids: []
